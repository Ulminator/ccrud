{
  "name": "ccrud",
  "version": "1.0.0",
  "description": "This is a tool that generates an Express application with endpoints that handle CRUD for entities you define.",
  "main": "./lib/index.js",
  "scripts": {
    "setup": "sh ./test/postgres/init.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["http", "REST", "CRUD", "express"],
  "author": "Ulminator",
  "license": "MIT",
  "bin": {
    "ccrud": "./lib/index.js"
  },
  "dependencies": {
    "express": "^4.16.4",
    "pg": "^7.9.0",
    "rimraf": "^2.6.3"
  }
}
